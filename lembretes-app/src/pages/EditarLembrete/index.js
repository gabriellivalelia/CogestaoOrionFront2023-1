import React, { useState } from 'react'
import { View, TextInput, Text, Button } from 'react-native'

export default function EditarLembrete({ navigation }){

    return(
        <View>
            <Text>Editar Lembrete</Text>
        </View>
    );
}